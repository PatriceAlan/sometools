[project]
name = "sometools"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "click>=8.1.7",
    "ruff>=0.7.1",
]
[project.scripts]
tools-cli = "cli:cli"

